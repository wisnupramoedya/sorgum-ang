import { IfRolesDirective } from './if-roles.directive';

describe('IfRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new IfRolesDirective();
    expect(directive).toBeTruthy();
  });
});
